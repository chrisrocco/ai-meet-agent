---
phase: 09-error-handling-and-distribution-readiness
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - package.json
  - .npmignore
  - config.example.json
  - LICENSE
  - README.md
autonomous: true
requirements:
  - ERR-01
  - ERR-02
  - ERR-03

must_haves:
  truths:
    - "npm pack --dry-run lists only production files (no .planning, no test files)"
    - "config.example.json shows all configurable fields with sensible defaults"
    - "README.md explains installation, usage, configuration, and troubleshooting"
    - "package.json has no private:true, has repository/keywords/license fields"
  artifacts:
    - path: "config.example.json"
      provides: "Example configuration with comments-as-fields for all options"
    - path: ".npmignore"
      provides: "Excludes planning docs, test files, dev artifacts from npm package"
    - path: "LICENSE"
      provides: "MIT license file"
    - path: "README.md"
      provides: "Installation, usage, configuration, troubleshooting docs"
    - path: "package.json"
      provides: "npm-publishable metadata"
      contains: "repository"
  key_links:
    - from: "README.md"
      to: "config.example.json"
      via: "Configuration section references example file"
      pattern: "config.example.json"
    - from: "README.md"
      to: "package.json"
      via: "Install instructions use package name"
      pattern: "npm install -g ai-meet-agent"
---

<objective>
Make the package ready for `npm install -g` distribution with proper metadata, documentation, and packaging.

Purpose: Complete the distribution readiness aspect of Phase 9 — a first-time user can install, configure, and troubleshoot using only the published package contents.
Output: README.md, LICENSE, config.example.json, .npmignore, updated package.json.
</objective>

<execution_context>
@/home/chris/.claude/get-shit-done/workflows/execute-plan.md
@/home/chris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-error-handling-and-distribution-readiness/09-RESEARCH.md
@.planning/phases/09-error-handling-and-distribution-readiness/09-01-SUMMARY.md

<interfaces>
<!-- Key info the executor needs from package.json -->

From package.json (current):
```json
{
  "name": "ai-meet-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "ai-meet": "./bin/ai-meet.ts" },
  "engines": { "node": ">=22" },
  "dependencies": {
    "@google/genai": "^1.46.0",
    "commander": "^14.0.3",
    "fluent-ffmpeg": "^2.1.3",
    "tsx": "^4.21.0",
    "zod": "^3.23.8"
  }
}
```

From src/config/schema.ts (all configurable fields):
```typescript
ConfigSchema = z.object({
  devices: { camera: { label, videoNr, imagePath }, mic: { label, sinkName }, sink: { label, sinkName } },
  audio: { relayPort },
  wsl2: { captureDevice, outputDevice, ffmpegPath, ffplayPath },
  video: { mjpegPort },
  persona: { name, role, background, instructions, introduceOnStart },
  ai: { model }
})
```

Error exit codes (from src/errors/index.ts):
- 1: Generic agent error
- 2: Configuration error
- 3: Device error (missing deps)
- 4: AI session error
- 5: Audio pipeline error
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Distribution artifacts — .npmignore, config.example.json, LICENSE</name>
  <files>.npmignore, config.example.json, LICENSE, package.json</files>
  <action>
    **1. Create `.npmignore`:**
    ```
    # Planning and documentation (development only)
    .planning/
    .claude/
    .agents/
    docs/
    scripts/

    # Test files
    src/**/*.test.ts
    test-*.mjs
    test-*.ts

    # Dev artifacts
    .env
    .env.*
    transcript.log
    meeting-notes.md
    tsconfig.json

    # Assets that shouldn't ship (placeholder image ships via src/video/)
    # None currently

    # Git/editor
    .git/
    .gitignore
    ```

    **2. Create `config.example.json`:**
    Include all fields from ConfigSchema with their defaults, plus inline comments as JSON field names where helpful. Structure:
    ```json
    {
      "persona": {
        "name": "AI Assistant",
        "role": "Meeting Participant",
        "background": "A helpful AI that participates in meetings",
        "instructions": "Be concise and professional. Listen actively.",
        "introduceOnStart": true
      },
      "ai": {
        "model": "gemini-2.5-flash-native-audio-latest"
      },
      "devices": {
        "camera": {
          "label": "AI Meet Agent Camera",
          "videoNr": 10
        },
        "mic": {
          "label": "AI Meet Agent Mic",
          "sinkName": "ai_meet_mic"
        },
        "sink": {
          "label": "AI Meet Agent Sink",
          "sinkName": "ai_meet_sink"
        }
      },
      "audio": {
        "relayPort": 19876
      },
      "video": {
        "mjpegPort": 8085
      },
      "wsl2": {
        "captureDevice": "CABLE Output (VB-Audio Virtual Cable)",
        "outputDevice": "Voicemeeter Input (VB-Audio Voicemeeter VAIO)",
        "ffmpegPath": "ffmpeg.exe",
        "ffplayPath": "ffplay.exe"
      }
    }
    ```

    **3. Create `LICENSE`:**
    MIT license, copyright 2026, holder name from git config or "AI Meet Agent Contributors".

    **4. Update `package.json`:**
    - Remove `"private": true`
    - Add `"license": "MIT"`
    - Add `"repository": { "type": "git", "url": "https://github.com/user/ai-meet-agent.git" }` (use placeholder if no real repo URL found in git remote)
    - Add `"keywords": ["ai", "meeting", "google-meet", "gemini", "audio", "virtual-devices", "cli"]`
    - Add `"description": "AI meeting twin — bidirectional realtime audio through Google Meet"` (if not already present)
    - Keep `"bin"` pointing to `"./bin/ai-meet.ts"` (tsx shebang works since tsx is a dependency)
    - Add `"files": ["bin/", "src/", "assets/", "package.json", "LICENSE", "README.md", "config.example.json"]` to explicitly control what npm publishes
  </action>
  <verify>
    <automated>npm pack --dry-run 2>&1 | head -40</automated>
  </verify>
  <done>npm pack lists only production files. No .planning, test files, or dev artifacts in the tarball. LICENSE and config.example.json included.</done>
</task>

<task type="auto">
  <name>Task 2: README.md with installation, usage, configuration, and troubleshooting</name>
  <files>README.md</files>
  <action>
    Create `README.md` at project root with the following sections. Keep it practical and scannable — this is a CLI tool README, not a thesis.

    **Structure:**

    ```markdown
    # AI Meet Agent

    AI meeting twin — bidirectional realtime audio conversation through Google Meet. Someone speaks, the AI twin hears and responds naturally.

    ## Prerequisites

    - Node.js >= 22
    - Linux (native or WSL2)
    - Google Gemini API key (`GEMINI_API_KEY`)
    - ffmpeg (`sudo apt install ffmpeg`)
    - PipeWire/PulseAudio (typically pre-installed on modern Linux)

    ### Native Linux Only
    - v4l2loopback kernel module (for virtual camera)

    ### WSL2 Only
    - OBS Virtual Camera (Windows side)
    - VB-Audio Virtual Cable (Windows side)

    ## Installation

    ```bash
    npm install -g ai-meet-agent
    ```

    Or run without installing:

    ```bash
    npx ai-meet-agent start
    ```

    ## Quick Start

    1. Set your API key:
       ```bash
       export GEMINI_API_KEY=your-key-here
       ```

    2. (Optional) Copy and edit the example config:
       ```bash
       cp node_modules/ai-meet-agent/config.example.json ./config.json
       ```

    3. Start a session:
       ```bash
       ai-meet start
       ```

    4. Open Google Meet, select "AI Meet Agent Camera" and "AI Meet Agent Mic" as your devices.

    ## Commands

    | Command | Description |
    |---------|-------------|
    | `ai-meet start` | Launch a meeting session |
    | `ai-meet start --config <path>` | Use a specific config file |
    | `ai-meet start --notes <path>` | Load meeting context from markdown |
    | `ai-meet start --role <path>` | Load persona from a role file |
    | `ai-meet start --verbose` | Enable verbose audio level logging |
    | `ai-meet list-devices` | Show available audio/video devices |
    | `ai-meet test-audio` | Verify audio device setup |
    | `ai-meet --version` | Show version |
    | `ai-meet --help` | Show help |

    ## Configuration

    All fields are optional — sensible defaults are used when omitted. See `config.example.json` for the full schema.

    ### Key Fields

    | Field | Default | Description |
    |-------|---------|-------------|
    | `persona.name` | "AI Assistant" | Name used in transcripts |
    | `persona.role` | "Meeting Participant" | Role context for AI |
    | `persona.instructions` | "" | Custom behavior instructions |
    | `ai.model` | gemini-2.5-flash-native-audio-latest | Gemini model ID |
    | `devices.camera.videoNr` | 10 | v4l2loopback device number |

    ### Environment Variables

    | Variable | Required | Description |
    |----------|----------|-------------|
    | `GEMINI_API_KEY` | Yes | Google Gemini API key |

    `.env` files in the working directory are loaded automatically.

    ## Error Codes

    | Code | Meaning | Common Fix |
    |------|---------|------------|
    | 2 | Config error | Check config.json fields — error names the bad field |
    | 3 | Missing dependency | Install the dependency shown in the error hint |
    | 4 | AI session error | Check GEMINI_API_KEY and network connection |
    | 5 | Audio pipeline error | Check PulseAudio/PipeWire setup |

    ## Troubleshooting

    **"ffmpeg: command not found"**
    ```bash
    sudo apt install ffmpeg
    ```

    **"v4l2loopback kernel module not loaded"**
    ```bash
    sudo modprobe v4l2loopback video_nr=10 card_label="AI Meet Agent Camera" exclusive_caps=1 max_buffers=2
    ```

    **"GEMINI_API_KEY not set"**
    ```bash
    export GEMINI_API_KEY=your-key
    # Or create a .env file: echo "GEMINI_API_KEY=your-key" > .env
    ```

    **WSL2 audio not working**
    See `docs/wsl2-setup.md` for Windows-side VB-Cable and OBS configuration.

    ## License

    MIT
    ```

    Keep the formatting clean. Use actual backtick fences, not escaped ones. Do not add badges, shields, or decorative elements.
  </action>
  <verify>
    <automated>test -f README.md && head -5 README.md | grep -q "AI Meet Agent" && echo "OK"</automated>
  </verify>
  <done>README.md exists with installation, usage, configuration, error codes, and troubleshooting sections. References config.example.json.</done>
</task>

</tasks>

<verification>
- `npm pack --dry-run` lists bin/, src/, assets/, LICENSE, README.md, config.example.json — no .planning or test files
- `cat package.json | grep -c '"private"'` returns 0 (removed)
- `cat package.json | grep -q '"license"'` succeeds
- `cat README.md | grep -q "npm install -g"` succeeds
- `test -f LICENSE && test -f config.example.json && test -f .npmignore` all succeed
</verification>

<success_criteria>
- Package is publishable: no private flag, has license, has description, has repository
- npm pack includes only production files
- README covers: prerequisites, installation, quick start, commands, configuration, error codes, troubleshooting
- config.example.json matches ConfigSchema with all fields and defaults
- LICENSE is MIT
</success_criteria>

<output>
After completion, create `.planning/phases/09-error-handling-and-distribution-readiness/09-02-SUMMARY.md`
</output>
